// import 'package:flutter_test/flutter_test.dart';

// bool isEmailValid(String email) => RegExp(r'^[\w-\.]+@([\w-]+\.)+[\w-]{2,4}\$').hasMatch(email);
// bool isPasswordValid(String password) => password.length >= 6;

// void main() {
//   group('Register Form Validation', () {
//     test('Valid email passes regex', () {
//       expect(isEmailValid('test@example.com'), isTrue);
//     });

//     test('Invalid email fails regex', () {
//       expect(isEmailValid('invalid-email'), isFalse);
//     });

//     test('Password of length 6 passes', () {
//       expect(isPasswordValid('123456'), isTrue);
//     });

//     test('Short password fails', () {
//       expect(isPasswordValid('123'), isFalse);
//     });
//   });
// }
